name: Generate UML diagrams

on:
  push:
  #   branches: [ master ]
  # pull_request:
  #   branches: [ master ]

jobs:
  generate-uml:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-python@v2
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: pip3 install hpp2plantuml

    - name: Do do job
      run: |
        echo ok

